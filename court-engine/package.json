{
  "name": "court-engine",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Local Xano-compatible court runtime: matching, drift classification, severity, cases, verdicts, precedents.",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
