# Xano static frontend

Vite + React courtroom. Talks to `/api` locally (proxied to `court-engine`) or to `VITE_XANO_API_BASE` when hosted on Xano.

```bash
npm run dev     # from repo root
npm run build -w static
```
